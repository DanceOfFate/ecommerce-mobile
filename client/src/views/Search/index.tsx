import {Text, View} from "react-native";
import {SafeAreaView} from "react-native-safe-area-context";

export const Search = () => {
    return(
        <SafeAreaView>
            <Text>Search</Text>
        </SafeAreaView>
    )
}