import {SafeAreaView} from "react-native-safe-area-context";
import {Text} from "react-native";

export const Cart = () => {
    return(
        <SafeAreaView>
            <Text>Cart</Text>
        </SafeAreaView>
    )
}